<!--miniprogram/pages/editUserInfo/signature/signature.wxml-->

<!-- <button bindtap="handleBtn">设置个性签名</button>
<input type="text" value="{{ signature }}" bindinput="handleText" /> -->
<view class="page-section">
    <!-- <view class="weui-cells__title">设置个性签名</view> -->
    <button class="weui-cells__title" bindtap="handleBtn">设置个性签名</button>
    <view class="weui-cells weui-cells_after-title">
      <view class="weui-cell weui-cell_input">
        <input class="weui-input" maxlength="100" placeholder="请输入你的签名" value="{{ signature }}" bindinput="handleText"/>
      </view>
    </view>
  </view>
<button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
        disabled="{{disabled}}" bindtap="handleBtn"> 确认 </button>

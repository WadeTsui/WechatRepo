<!--miniprogram/pages/editUserInfo/phone/phone.wxml-->

<!-- <button bindtap="handleBtn">设置手机号</button>
<input type="number" value="{{ phoneNumber }}" bindinput="handleText" /> -->

<view class="page-section">
    <!-- <view class="weui-cells__title">设置手机号</view> -->
    <button bindtap="handleBtn">设置手机号</button>
    <view class="weui-cells weui-cells_after-title">
      <view class="weui-cell weui-cell_input">
        <input class="weui-input" type="number" maxlength="11" placeholder="请输入你的手机号" value="{{ phoneNumber }}" bindinput="handleText"/>
      </view>
    </view>
  </view>
<button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
        disabled="{{disabled}}" bindtap="handleBtn"> 确认 </button>